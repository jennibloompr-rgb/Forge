# Where partner-gate leads are delivered. Point this at a form service
# (Formspree, Basin) or your own endpoint that accepts a JSON POST of:
#   { email, source, page, timestamp, userAgent }
# If unset, leads are written to the server logs so they are never lost.
FORGE_LEAD_WEBHOOK=
